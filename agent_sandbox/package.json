{
  "name": "truthlens-agent-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "Epistemic sandbox runtime for TruthLens pipeline — Node.js + container isolation layer",
  "main": "dist/runtime.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "run-pipeline": "tsx src/runtime.ts",
    "validate-audit": "tsx src/audit.ts validate"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
